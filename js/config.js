/**
 * Created by sech on 30.05.14.
 * config file holding the configuration for the visualization
 */

var config = {
    // show only tags / facet values separately which are assigned more results than indicated by this value
    // values with less results are subsumed under the tag "others"
    MIN_TAG_FREQUENCY_FOR_DISPLAY : 3,
    // name of the facet values where all values with only few results are aggregated into
    TAG_NAME_OTHERS: 'others',
    TAG_MAX_LENGTH : 30
}

